apiVersion: v1
kind: Service
metadata:
  name: {{ include "weather-wms.fullname" . }}-data-pipeline-ingester
  labels:
    {{- include "weather-wms.labels" . | nindent 4 }}
    app.kubernetes.io/component: data-pipeline
spec:
  type: ClusterIP
  ports:
    - port: 8082
      targetPort: http-ing
      name: http
  selector:
    {{- include "weather-wms.selectorLabels" . | nindent 4 }}
    app.kubernetes.io/component: data-pipeline
