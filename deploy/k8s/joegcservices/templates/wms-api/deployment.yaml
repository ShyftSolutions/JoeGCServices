apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "joegcservices.fullname" . }}-wms-api
  labels:
    {{- include "joegcservices.labels" . | nindent 4 }}
    app.kubernetes.io/component: wms-api
spec:
  replicas: {{ .Values.wmsApi.replicaCount }}
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxUnavailable: 0
      maxSurge: 1
  selector:
    matchLabels:
      {{- include "joegcservices.selectorLabels" . | nindent 6 }}
      app.kubernetes.io/component: wms-api
  template:
    metadata:
      labels:
        {{- include "joegcservices.selectorLabels" . | nindent 8 }}
        app.kubernetes.io/component: wms-api
    spec:
      containers:
        - name: wms-api
          image: {{ include "joegcservices.image" (dict "registry" .Values.global.image.registry "repository" .Values.wmsApi.image.repository "tag" .Values.wmsApi.image.tag) }}
          imagePullPolicy: {{ .Values.global.image.pullPolicy }}
          ports:
            - containerPort: 8080
              name: http
          env:
            - name: CONFIG_DIR
              value: "/app/config"
            - name: DATABASE_URL
              valueFrom:
                secretKeyRef:
                  name: {{ include "joegcservices.fullname" . }}-secrets
                  key: DATABASE_URL
            - name: REDIS_URL
              valueFrom:
                secretKeyRef:
                  name: {{ include "joegcservices.fullname" . }}-secrets
                  key: REDIS_URL
            - name: S3_ENDPOINT
              valueFrom:
                secretKeyRef:
                  name: {{ include "joegcservices.fullname" . }}-secrets
                  key: S3_ENDPOINT
            - name: S3_BUCKET
              value: {{ .Values.minio.bucket | quote }}
            - name: S3_ACCESS_KEY
              valueFrom:
                secretKeyRef:
                  name: {{ include "joegcservices.fullname" . }}-secrets
                  key: S3_ACCESS_KEY
            - name: S3_SECRET_KEY
              valueFrom:
                secretKeyRef:
                  name: {{ include "joegcservices.fullname" . }}-secrets
                  key: S3_SECRET_KEY
            - name: S3_REGION
              value: "us-east-1"
            - name: S3_ALLOW_HTTP
              value: "true"
            # Ingester proxy — points to the co-located data pipeline pod
            - name: INGESTER_URL
              value: "http://{{ include "joegcservices.fullname" . }}-data-pipeline-ingester:8082"
            {{- if .Values.wmsApi.cleanup.separate }}
            # When cleanup runs in a separate pod, disable on main replicas
            - name: ENABLE_CLEANUP
              value: "false"
            - name: ENABLE_SYNC
              value: "false"
            {{- end }}
            {{- range $key, $value := .Values.wmsApi.env }}
            {{- if and (eq $key "ENABLE_CLEANUP") $.Values.wmsApi.cleanup.separate }}
            {{- /* Skip — already set above */ -}}
            {{- else if and (eq $key "ENABLE_SYNC") $.Values.wmsApi.cleanup.separate }}
            {{- /* Skip — already set above */ -}}
            {{- else }}
            - name: {{ $key }}
              value: {{ $value | quote }}
            {{- end }}
            {{- end }}
          resources:
            {{- toYaml .Values.wmsApi.resources | nindent 12 }}
          livenessProbe:
            httpGet:
              path: /health
              port: http
            initialDelaySeconds: 30
            periodSeconds: 15
            timeoutSeconds: 10
            failureThreshold: 5
          readinessProbe:
            httpGet:
              path: /wms?SERVICE=WMS&REQUEST=GetCapabilities
              port: http
            initialDelaySeconds: 10
            periodSeconds: 10
            timeoutSeconds: 15
            failureThreshold: 5
