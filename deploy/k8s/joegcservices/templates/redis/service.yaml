apiVersion: v1
kind: Service
metadata:
  name: {{ include "joegcservices.fullname" . }}-redis
  labels:
    {{- include "joegcservices.labels" . | nindent 4 }}
    app.kubernetes.io/component: redis
spec:
  type: ClusterIP
  ports:
    - port: 6379
      targetPort: redis
      name: redis
  selector:
    {{- include "joegcservices.selectorLabels" . | nindent 4 }}
    app.kubernetes.io/component: redis
