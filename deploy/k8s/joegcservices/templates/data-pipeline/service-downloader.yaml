apiVersion: v1
kind: Service
metadata:
  name: {{ include "joegcservices.fullname" . }}-data-pipeline-downloader
  labels:
    {{- include "joegcservices.labels" . | nindent 4 }}
    app.kubernetes.io/component: data-pipeline
spec:
  type: ClusterIP
  ports:
    - port: 8081
      targetPort: http-dl
      name: http
  selector:
    {{- include "joegcservices.selectorLabels" . | nindent 4 }}
    app.kubernetes.io/component: data-pipeline
